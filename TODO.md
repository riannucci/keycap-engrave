# TODO

* Add feature to do 'engrave' mode (for SLS/SLA) were the engraving is just a
thicker pancake without projecting and reorienting the extrusion. Implies
insert omission (per-legend)
* Finish TOML->Preview->render pipeline scripts
* Ensure this works with MX style keycaps.
* Give it the ability to engrave on surfaces other than the face (e.g. front
printed). Probably good to combo with engrave.
* Add batch sorting options to toml so that it dumps caps into deterministic
batchN folders of size B, organized by cap type, legend colors and orientation.
* Add proper README.md.
