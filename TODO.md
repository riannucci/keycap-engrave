# TODO

* Add feature to omit colored inserts (per-legend).
* Add feature to do 'engrave' mode (for SLS/SLA) were the engraving is just a thicker pancake without projecting and reorienting the extrusion. Implies insert omission (per-legend)
* Make variable names nicer in script.
* Finish TOML->Preview->render pipeline scripts
* Ensure this works with MX style keycaps.
* Give it the ability to engrave on surfaces other than the face (e.g. front printed). Probably good to combo with engrave.
